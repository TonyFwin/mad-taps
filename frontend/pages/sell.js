import React from 'react';
import CreateBeer from '../components/CreateBeer';

// TODO: check if user is brewery before creating beer

const Sell = props => {
  return (
    <div>
      <CreateBeer />
    </div>
  );
};

export default Sell;
